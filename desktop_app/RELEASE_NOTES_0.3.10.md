# VarioField 0.3.10

## Sprint C — Contraste, tokens, foco teclado

- `textDim` actualizado a `#8592a8` (WCAG AA ≥4.5:1 sobre fondo/superficie)
- Placeholders unificados con `colors.textPlaceholder` (sin `#6b7280` suelto)
- **Catálogo VDF** (`CatalogEditor`) migrado al design system: tokens + `touchMin` 48
- Chips de `map_status` con área táctil ≥48 px
- Anillo de foco (`borderFocus`) en Chip, botón ayuda y links secundarios
- `hitSlop` en ayuda y links pequeños
- Export web: `lang: "es"` en `app.json`

Incluye Sprints A–B (seguridad UX + labels/live regions).

## Binarios
- `VarioField-0.3.10-arm64.AppImage`
- `VarioField-0.3.10-x86_64.AppImage`
- `VarioField-Setup-0.3.10.exe`
