# Windows Setup — SAJ PDM-30 Gestor

Genera `SAJ_PDM30_Gestor_Setup.exe` con NSIS (compilable en Linux).

```bash
cd desktop_app
./build_gestor_windows_setup.sh
# → dist/windows/SAJ_PDM30_Gestor_Setup.exe
```

Requisito de build: `sudo apt install nsis`
